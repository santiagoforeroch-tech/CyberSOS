# Último handoff

- **Actualización 2026-09-08 (analítica y WhatsApp):** El panel administrativo ahora representa la evolución de reportes de los últimos siete días y gráficos circulares de estados y prioridades, usando únicamente el resumen que ya entrega FastAPI. Son adaptables a móvil y muestran mensajes útiles cuando aún no hay datos. El puente Baileys acepta tanto la variable heredada `SUPABASE_SERVICE_ROLE_KEY` como la clave secreta actual `SUPABASE_SECRET_KEY`; sus pruebas ya usan pnpm, evitando la dependencia de npm. El comprobador de la integración confirmó estructura y límites básicos correctos.
- **Pendiente de WhatsApp real:** El QR no se inicia con SQLite: requiere PostgreSQL/Supabase accesible, una cuenta dedicada y secretos privados de cifrado y firma. No se vinculó ningún número durante esta mejora.

- **Actualización 2026-09-08 (modo local confiable):** El acceso `Abrir CyberSOS` fuerza una base SQLite de desarrollo solo para sus procesos locales, crea las tablas antes de iniciar y comprueba mediante `/api/health` que la instancia usa SQLite. La URL de Supabase de `.env` no se modifica y se reserva para el servidor de despliegue. El endpoint de salud informa el tipo de base sin revelar datos sensibles.
- **Verificación final 2026-09-08:** Pytest 7/7 correcto con bases temporales; ESLint correcto; Vitest 4/4 correcto; build de Vite correcto. Se inició la aplicación local y se registró un reporte ficticio de integración (`201`), mientras `/api/health` confirmó `status: ok` y `database: sqlite`. Los registros de verificación temporales permanecen dentro de `.runtime`, ignorado por Git.

- **Actualización 2026-09-08 (robustez administrativa):** La bandeja permite filtrar también por prioridad y actualizar datos de forma manual, además de la sincronización automática. El detalle distingue entre sesión vencida, caso inexistente y fallos temporales; al guardar cambios u observaciones ahora muestra el error en lugar de fallar silenciosamente. La capa HTTP conserva el código de estado para que la interfaz pueda tomar esas decisiones. Las pruebas usan SQLite temporal aislado y ya no dependen de la base configurada en `.env`.
- **Verificación 2026-09-08:** ESLint correcto; Pytest 7/7; `GET /api/health` respondió `{"status":"ok"}`; escaneo de secretos sin hallazgos y `.env` ignorado por Git. Vitest y el build de Vite continúan bloqueados por el permiso de lectura de esbuild sobre el directorio superior de OneDrive, no por una falla detectada en la interfaz.

- **Fecha:** 2026-09-07
- **Objetivo actual:** Transformar CyberSOS de una demostración de piloto a una plataforma institucional de ciberdenuncias.
- **Terminado:** Se retiraron la pantalla y API de código de piloto, Gemini y su consentimiento. El formulario web recibe denuncias sin cuenta ciudadana; la cuenta administrativa sigue siendo única y exige MFA. La bandeja actualiza reportes y métricas automáticamente cada 15 segundos. Se inició una nueva identidad visual violeta y magenta.
- **Pendiente:** Verificar frontend, backend y flujo en navegador; añadir controles antiabuso antes de admitir datos reales. No se debe habilitar registro público de administradores ni ciudadanos.
- **Decisión:** La tabla histórica de análisis IA no se elimina en esta entrega para evitar una migración destructiva; ya no hay endpoint ni interfaz que la use.
- **Pruebas:** Pytest 6/6 correcto y ESLint correcto. La prueba visual de esta carpeta quedó bloqueada porque `localhost:5173` está atendido por otra copia antigua ubicada en `Apolo`; su proceso no se modificó. Vite/esbuild también encuentra restricciones de lectura del entorno al compilar desde OneDrive.
- **Actualización 2026-09-07:** Se creó el símbolo SVG propio de CyberSOS, la guía de identidad “Vigilancia Violeta”, y métricas de categorías, prioridades y promedio de gestión para el panel. Se añadió un límite en memoria de 5 envíos por IP cada hora para el formulario público. Resultado: Pytest 7/7 y ESLint correcto. Este límite es una primera defensa para desarrollo y debe complementarse con controles perimetrales antes de producción.
- **Actualización 2026-09-07 (acceso):** Se incorporó una ilustración original de baliza protegida por una red digital en el inicio de sesión administrativo. El formulario conserva etiquetas, contraste y acceso directo a creación de cuenta. La vista se comprobó en navegador en `http://127.0.0.1:4174/admin/login`; ESLint correcto.
- **Actualización 2026-09-07 (diseño de acceso):** El acceso administrativo se reorganizó siguiendo una composición institucional: identidad e ilustración a la izquierda y tarjeta de inicio de sesión a la derecha. En pantallas pequeñas pasa a una sola columna, mantiene controles táctiles amplios y evita desplazamiento horizontal. La creación de cuenta sigue limitada al único administrador autorizado.
- **Actualización 2026-09-07 (ilustración de acceso):** El panel visual del acceso incorpora una retícula digital, nodos de red, luz ambiental e indicador de reportes protegidos sobre la ilustración. Los elementos decorativos se ocultan en móvil para mantener la pantalla clara y ligera.
- **Actualización 2026-09-08 (arranque y reportes):** Los accesos locales ahora inician la versión actual en `http://localhost:4174`, permiten CORS desde ese puerto y crean las tablas SQLite de desarrollo antes de arrancar FastAPI. Se verificó `GET /api/health`, un reporte ficticio válido (`201`) y el mensaje específico para un correo inválido (`422`). Los intentos inválidos ya no consumen el límite antiabuso.
- **Actualización 2026-09-08 (WhatsApp):** El panel QR conserva credenciales administrativas al consultar FastAPI, muestra el carácter local del canal y explica la indisponibilidad de la central sin revelar información sensible. Las tablas de WhatsApp quedan incluidas al inicializar SQLite para desarrollo. El QR real requiere configurar PostgreSQL/Supabase, `SUPABASE_SERVICE_ROLE_KEY`, `WHATSAPP_SESSION_ENCRYPTION_KEY` y `WHATSAPP_WEBHOOK_SECRET` únicamente en `.env`; no hay valores de esos secretos en esta copia, por lo que no se inició ni vinculó una cuenta.
- **Actualización 2026-09-08 (formulario público):** Cada categoría de incidente ahora incluye una explicación breve y no técnica —por ejemplo, phishing, suplantación, extorsión y malware— para que una persona pueda elegir su caso sin conocer vocabulario especializado.

- **Fecha:** 2026-09-05
- **Objetivo actual:** Completar la integración local de WhatsApp y Gemini con datos ficticios.
- **Terminado:** Se instaló la skill `integrar-whatsapp-baileys` y se añadió el puente Baileys, almacenamiento privado, outbox, eventos firmados, panel local protegido por sesión administrativa y migración revisable. Los mensajes directos nuevos crean o actualizan un reporte de origen WhatsApp y dejan historial. Gemini propone categoría, prioridad, resumen y señales con salida estructurada; no recibe datos de contacto, exige consentimiento y nunca modifica el caso por sí mismo.
- **Pendiente:** Aplicar la migración `20260831000000_create_whatsapp_central.sql` mediante Supabase MCP, configurar secretos solo en `.env`, instalar/probar el puente y enlazar una cuenta dedicada. Configurar una clave Gemini local para la prueba real. No conectar números ni usar datos reales hasta contar con aprobación.
- **Actualización 2026-09-05:** La migración `20260831000000_create_whatsapp_central.sql` se aplicó manualmente en el SQL Editor del proyecto Supabase de desarrollo `byqympyzppstmgofqohs`. La consola devolvió éxito y la consulta posterior mostró tablas `whatsapp_*`, incluido `whatsapp_connections`; no se conectó ningún número. Verificación local posterior: comprobador de la integración correcto, Pytest 5/5 y puente TypeScript 2/2. La verificación local del frontend quedó pendiente: el runtime aislado no pudo resolver `frontend/vite.config.js` por permisos de lectura del directorio superior, pese a que el archivo existe.
- **Actualización de verificación 2026-09-05:** Se añadió una prueba automática del webhook: con un evento ficticio firmado, FastAPI devuelve 202, evita procesar por segunda vez el mismo `event_id` y crea un reporte de origen WhatsApp. Resultado actual: Pytest 6/6, puente TypeScript 2/2, ESLint correcto, Vitest 3/3, build de Vite correcto y `/api/health` respondió `{"status":"ok"}` en localhost. Se corrigió `frontend/pnpm-workspace.yaml` para permitir explícitamente el binario bloqueado de `esbuild` durante el build local. `.env` sigue fuera de Git y el escaneo no detectó secretos fuera de `.env.example`.
- **Pruebas:** `pytest` 5/5; comprobador de la skill de WhatsApp correcto; SDK Google GenAI importado correctamente. Frontend: ESLint correcto, Vitest 3/3 y build de Vite correcto. Puente: TypeScript correcto y pruebas Node 2/2. La integración con una cuenta y Gemini real no se ejecutó porque todavía no hay migración aplicada ni secretos configurados.
- **Bloqueos:** El MCP de Supabase no está disponible en esta sesión para aplicar la migración. Node quedó disponible mediante pnpm y el puente está verificado; no se usa `npm` porque no está instalado en el runtime.

- **Fecha:** 2026-08-28
- **Objetivo actual:** Enrolar el factor TOTP y completar la prueba real del acceso administrativo con MFA.
- **Terminado:** Dependencias, UI/UX Pro Max 2.15.0, esquema privado aplicado en Supabase, API de piloto/reportes, número de caso, portada dinámica, bandeja administrativa, detalle e historial. Supabase Auth quedó integrado con cookies seguras, enrolamiento TOTP y exigencia de nivel `aal2`. El 2026-08-27 se autorizó e invitó a `santiagoforeroch@gmail.com` como único administrador del panel y se confirmó que también es propietario técnico de APOLO.
- **Terminado adicional:** Flujo de evidencias con validación de firma, límite de 10 MB, máximo cinco archivos, SHA-256, nombres aleatorios, bucket privado y enlaces de descarga de 60 segundos. El detalle administrativo ya presenta las evidencias. Se aplicaron índices para evidencias, análisis, observaciones e historial.
- **Terminado adicional el 2026-08-28:** La clave secreta del backend quedó configurada solo en `.env`. Se verificó contra Supabase una carga ficticia, un enlace temporal, la descarga con contenido idéntico y la limpieza del archivo de prueba.
- **Terminado adicional de autenticación:** Una consulta administrativa de solo lectura confirmó que el administrador existe y que su correo ya está confirmado. No se mostraron identificadores ni datos sensibles.
- **Pendiente:** Enrolar el factor TOTP y completar la prueba real con MFA, activar antivirus antes de datos reales, análisis Gemini, webhook de WhatsApp y retención automática.
- **Pruebas:** Pytest 2/2 con SQLite temporal (incluye `/api/health`, evidencia válida, rechazo de archivo falso y enlace temporal), Vitest 2/2, ESLint sin advertencias y build de producción correcto. La prueba real del bucket privado completó carga, URL firmada, descarga íntegra y limpieza. El escaneo no detectó secretos y `.env` sigue ignorado por Git.
- **Seguridad:** El asesor de Supabase detecta protección contra contraseñas filtradas desactivada; debe evaluarse antes de producción. `.env` permanece ignorado y el escaneo no mostró secretos expuestos.
- **Acceso actualizado:** `santiagoforeroch@gmail.com` sustituyó al correo institucional como único administrador autorizado del panel y recibió una invitación de Supabase Auth. El usuario institucional anterior permanece sin autorización en la aplicación.
- **Bloqueos:** La verificación de solo lectura encontró cero factores MFA registrados. El administrador debe completar personalmente el enrolamiento TOTP; ninguna contraseña o código debe compartirse por chat.
- **Siguiente paso:** Abrir el panel, completar el enrolamiento TOTP y probar el acceso real; después, incorporar análisis antivirus.

No guardar secretos en este archivo.
