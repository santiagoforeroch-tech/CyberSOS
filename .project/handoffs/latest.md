# Último handoff

- **Actualización 2026-09-13 (corrección frontend):** Se corrigió el enlace “Cómo funciona” para que navegue al ancla real, se eliminaron opciones del login y del menú administrativo que no tenían implementación, el número de caso ahora se puede copiar con confirmación accesible, y los textos largos se parten sin montarse en tablas, detalles, cronologías o confirmaciones. El menú administrativo se reorganizó para móvil y se bloqueó el overflow horizontal causado por elementos decorativos. Verificación local: ESLint correcto, Vitest 6/6, build Vite correcto y Pytest 10/10 (solo queda un aviso de permisos de caché de pytest en OneDrive). Pendiente: publicar este último ajuste en Vercel y comprobar la URL pública.

- **Actualización 2026-09-13 (cierre técnico de producción):** Se añadieron encabezados CSP, `nosniff`, `DENY`, política de referer y permisos mínimos en `vercel.json`, además de un workflow de GitHub Actions que ejecuta Pytest, ESLint, Vitest y build antes de aceptar cambios. Se reforzó el contraste del login y se verificó en la URL pública con caché nuevo. Verificación local: backend 10/10, frontend 6/6, ESLint correcto, build Vite correcto, JSON de Vercel válido y escaneo de patrones de credenciales limpio. Queda pendiente únicamente la configuración/validación de credenciales externas y operaciones institucionales.

- **Actualización 2026-09-13 (Centro de protección ampliado):** `/proteccion` ahora incluye modo de riesgo inmediato, test local de tres preguntas con progreso, cinco guías por tipo de incidente, descarga de checklist, compartir/copia de enlace, preguntas frecuentes y enlaces a reportar. Frontend verificado con ESLint y 6 pruebas aprobadas.

- **Actualización 2026-09-13 (derivación oficial Colombia):** La confirmación de cada reporte ahora orienta explícitamente al ciudadano hacia CAI Virtual y ADenunciar para la denuncia formal, aclarando que CyberSOS es complementario y no un canal oficial. Se añadieron enlaces externos seguros y se verificó el frontend con 6 pruebas aprobadas.

- **Actualización 2026-09-13 (Centro de protección):** Se añadió la ruta pública `/proteccion` con recomendaciones accionables para mensajes sospechosos, cuentas y dispositivos; checklist desplegable; enlaces oficiales de Seguridad de Windows e INCIBE; y aviso de alcance para no presentarlo como sustituto de antivirus o autoridades. Se actualizó la navegación pública y las 6 pruebas frontend pasaron en `C:\dev\CyberSOS-Limpio`.

- **Actualización 2026-09-13 (antivirus y notificaciones):** Se añadió notificación SMTP segura, desactivada por defecto, y análisis opcional mediante ClamAV `INSTREAM`. Cuando `ANTIVIRUS_ENABLED=true`, una evidencia no se guarda si ClamAV no responde o detecta una amenaza. Las 10 pruebas de backend pasaron en `C:\dev\CyberSOS-Limpio`; falta desplegar/configurar el escáner privado y el SMTP de Hostinger.

- **Actualización 2026-09-13 (logo):** Se incorporó el archivo proporcionado por el estudiante como `frontend/src/assets/cybersos-logo.jpeg`. El componente de marca ahora lo reutiliza en portada, pie, acceso administrativo y panel, mostrando “CyberSOS” debajo del símbolo y tamaños adaptables. ESLint terminó correctamente. Vitest y build siguen bloqueados por el permiso de lectura de esbuild sobre el directorio superior de OneDrive; no se observó un error de código en esta modificación.
- **Actualización 2026-09-13 (consistencia de registro):** Se retiró del formulario de creación administrativa la ayuda antigua que exigía mayúsculas, minúsculas, números y símbolos, manteniendo la interfaz alineada con la validación vigente del backend.

- **Actualización 2026-09-12 (OTP por correo):** Se reemplazó el flujo TOTP con QR por códigos OTP enviados al correo registrado mediante Supabase Auth. Login, registro y activación solicitan el código por email; la sesión administrativa valida el rol `admin` y `setup_complete` sin exigir `aal2` de TOTP. Frontend lint correcto. Falta desplegar y probar el envío real con SMTP configurado.

- **Actualización 2026-09-12 (migraciones Supabase):** Se aplicaron correctamente las cinco migraciones existentes al proyecto de desarrollo `kmjlwaviqqznjagrgkpd`: núcleo CyberSOS, índices de evidencias y relaciones, central WhatsApp y auditoría de retención. Verificación: migraciones registradas, tablas principales creadas, conexión `public.whatsapp_connections` inicializada y `private.reports` con cero registros. El asesor reporta un aviso de RLS para tablas del esquema privado; la migración ya revoca `anon` y `authenticated`, por lo que no se aplicó una corrección automática que podría bloquear al backend.

- **Actualización 2026-09-12 (contraseñas abiertas):** Se eliminó del backend la validación de complejidad y longitud mínima para el registro administrativo; también se ocultó la ayuda visual que exigía mayúsculas, minúsculas, números y símbolos. Se conserva únicamente el límite máximo de 128 caracteres y la confirmación de coincidencia.

- **Actualización 2026-09-12 (contraseña fija de activación):** La creación única del administrador ahora valida la variable privada `ADMIN_SETUP_PASSWORD` en lugar de derivar la clave desde `SESSION_SECRET`. Se documentó únicamente el nombre de la variable en `.env.example` y `vercel.env.example`; no se guardó ninguna contraseña real.

- **Actualización 2026-09-12 (acceso local):** Se corrigió `scripts/abrir_cybersos.ps1`: Vite se inicia y ahora también se abre en `http://127.0.0.1:4174/`. Antes el script iniciaba en `127.0.0.1` pero abría `localhost`, que en este equipo rechazaba la conexión y mostraba una pantalla gris de error. Verificación visual directa: la portada CyberSOS cargó correctamente con la paleta violeta y magenta.

- **Actualización 2026-09-12 (configuración Vercel):** El despliegue de producción `cybersos-web` está `Ready`, procede de `master` (commit `b473f1c`) y la URL pública responde con salud PostgreSQL. Vercel registra `SUPABASE_URL`, claves Supabase, `DATABASE_URL`, `SESSION_SECRET`, `FRONTEND_ORIGIN`, `APP_ENV`, `AUTH_PROVIDER`, `COOKIE_SECURE` y correo administrador para producción/vista previa. `CRON_SECRET` no está configurado: la búsqueda de esa variable no devolvió resultados, por lo que la tarea diaria de retención responderá `503` y no debe considerarse activa. Los valores secretos no se visualizaron; el panel no permite confirmar desde la interfaz el `project_ref` de `SUPABASE_URL` sin revelar el valor.

- **Actualización 2026-09-12 (verificación remota Vercel/Supabase):** La URL pública `https://cybersos-web.vercel.app/` y `GET /api/health` respondieron `200`. La API desplegada devolvió `{"status":"ok","database":"postgresql"}`, confirmando que Vercel no usa SQLite local. Una consulta de solo lectura `SELECT 1` desde el backend configurado también respondió correctamente contra PostgreSQL de Supabase. Aún falta aplicar las migraciones al nuevo proyecto de desarrollo y verificar el flujo completo de formulario y administración en la URL pública.

- **Actualización 2026-09-12 (MCP Supabase):** El servidor `supabase` quedó registrado en la configuración global de Codex y verificado con alcance exclusivo al proyecto de desarrollo `kmjlwaviqqznjagrgkpd` (CyberSOS_1). La autenticación OAuth sigue fallando antes de abrir el consentimiento: Supabase rechaza el registro dinámico con `HTTP 400 Invalid scope`. En el navegador, las solicitudes OAuth generadas muestran la organización APOLO pero dejan deshabilitado el botón de autorización o vencen. No se modificaron tablas, datos ni secretos. No repetir intentos OAuth hasta contar con revisión técnica de compatibilidad Codex/Supabase.

- **Actualización 2026-09-12 (nuevo Supabase de desarrollo):** El estudiante confirmó que `kmjlwaviqqznjagrgkpd` será el proyecto de desarrollo de CyberSOS. La plantilla de entorno usa ahora su URL pública y ya no repite `SUPABASE_URL`. El backend desactiva prepared statements solo al detectar el pooler transaccional de Supabase (`:6543`), que no los admite. Aún falta introducir una `DATABASE_URL` completa con contraseña privada, verificar PostgreSQL y aplicar las migraciones existentes mediante Supabase MCP. No se trasladaron datos ni se modificó el proyecto anterior.

- **Actualización 2026-09-12 (conexión PostgreSQL):** La conexión al pooler transaccional del nuevo proyecto respondió correctamente mediante `psycopg` tras normalizar automáticamente la URL oficial `postgresql://` a `postgresql+psycopg://`. La base está vacía: la consulta de `private.reports` confirmó que el esquema aún no fue aplicado. Falta actualizar `SUPABASE_URL` y sus claves para que Auth/Storage correspondan al mismo proyecto nuevo, y conectar Supabase MCP para aplicar las migraciones; no se ejecutó SQL de esquema por otra vía.

- **Actualización 2026-09-11 (retención y antiabuso):** Se añadió una tarea diaria de Vercel (`03:17 UTC`) que llama a `/api/internal/retention/run`. El endpoint solo opera con el secreto privado `CRON_SECRET`, elimina exclusivamente reportes vencidos junto con sus evidencias privadas y deja el registro de auditoría; sin secreto responde `503` y no borra nada. Se añadió además un campo trampa invisible para filtrar bots genéricos en el formulario público. La plantilla `vercel.env.example` documenta el secreto nuevo sin incluir valores reales. Verificación: Pytest `9/9`, ESLint correcto, Vitest `6/6`, build Vite correcto, JSON de Vercel válido y escaneo de secretos limpio. Falta configurar las variables privadas y desplegar desde la cuenta autorizada; el MFA y la vinculación de WhatsApp deben hacerlos personalmente las cuentas responsables.

- **Actualización 2026-09-11 (formularios administrativos):** El cliente web ahora convierte la lista de errores de validación de FastAPI en texto legible, por ejemplo “Revisa el campo email: …”, en vez de mostrar `[object Object]`. La API pública respondió `422` ante una solicitud deliberadamente inválida, sin crear ni modificar cuentas; ESLint correcto. Vitest sigue bloqueado por el permiso de lectura de esbuild sobre el directorio superior de OneDrive, no por una falla de pruebas detectada.

- **Actualización 2026-09-11 (identidad visual):** Se reemplazó el símbolo anterior por un SVG plano y deliberadamente simple: escudo circular, punto de alerta ámbar y dos ondas de señal. Se retiró el degradado y la sombra para que funcione como una marca institucional diseñada, legible a tamaño pequeño y no como ilustración generativa. ESLint correcto. Falta publicar el cambio y comprobarlo en la URL pública.

- **Actualización 2026-09-11 (acceso administrativo):** La prueba con un usuario ficticio confirmó que Supabase responde y rechaza correctamente credenciales inválidas. El mensaje anterior confundía ese rechazo con una falla de conexión. El inicio de sesión ahora indica “Correo o contraseña incorrectos”; los problemas reales de configuración o conexión responden `503` con un mensaje distinto y quedan registrados solo en los logs del servidor. Pytest `7/7` correcto. Falta publicar el cambio y que el administrador complete personalmente la prueba real con contraseña y MFA.

- **Actualización 2026-09-11 (compatibilidad Vercel):** Vercel rechazó `experimentalServices` para proyectos nuevos. `vercel.json` ahora usa la sintaxis vigente `services`, con `frontend` y `backend` como raíces independientes y reglas explícitas que envían `/api/*` a FastAPI y el resto a Vite. La ruta llega a FastAPI conservando `/api`, por lo que el backend vuelve a declarar ese prefijo en todos los entornos. Verificación: JSON válido y Pytest `7/7`. El cambio debe enviarse a GitHub para que Vercel haga un nuevo despliegue; falta comprobar la URL pública.

- **Actualización 2026-09-10 (servicios Vercel):** El repositorio ahora define dos servicios de Vercel: Vite en `/` y FastAPI en `/api`. Se añadió `backend/main.py` como entrada ASGI y `backend/pyproject.toml` para dependencias de despliegue. En Vercel, la plataforma elimina `/api` antes de entregar la petición, por eso FastAPI usa rutas sin ese prefijo únicamente cuando está allí; localmente se mantienen las URLs `/api`. Verificación: importación ASGI correcta, `GET /health` en modo Vercel respondió `200` y Pytest `7/7`. Falta configurar en Vercel las variables privadas de Supabase, PostgreSQL, sesión y producción, y seleccionar el Framework Preset **Services** antes de verificar una URL pública.

- **Actualización 2026-09-10 (preparación Vercel):** Se añadió `vercel.json` para compilar el frontend Vite desde la raíz del repositorio y servir las rutas de React. El cliente HTTP dejó de asumir `localhost:8000` fuera de desarrollo: en un despliegue usa `/api` o la URL pública definida mediante `VITE_API_URL`. La configuración se publicó en GitHub con el commit `0b2c3ca`; el remoto canónico quedó en `santiagoforeroch-tech/CyberSOS`. Falta desplegar FastAPI en un servicio público y configurar esa URL y CORS antes de afirmar que el formulario está operativo en internet. Verificación de esta sesión: ESLint correcto. Vitest y build continúan limitados localmente: OneDrive bloquea esbuild en la carpeta original y la copia temporal no pudo completar la instalación de dependencias en este entorno. El escaneo de secretos no encontró valores expuestos; `.env` permanece ignorado.

- **Actualización 2026-09-08 (analítica y WhatsApp):** El panel administrativo ahora representa la evolución de reportes de los últimos siete días y gráficos circulares de estados y prioridades, usando únicamente el resumen que ya entrega FastAPI. Son adaptables a móvil y muestran mensajes útiles cuando aún no hay datos. El puente Baileys acepta tanto la variable heredada `SUPABASE_SERVICE_ROLE_KEY` como la clave secreta actual `SUPABASE_SECRET_KEY`; sus pruebas ya usan pnpm, evitando la dependencia de npm. El comprobador de la integración confirmó estructura y límites básicos correctos.
- **Pendiente de WhatsApp real:** El QR no se inicia con SQLite: requiere PostgreSQL/Supabase accesible, una cuenta dedicada y secretos privados de cifrado y firma. No se vinculó ningún número durante esta mejora.

- **Actualización 2026-09-08 (modo local confiable):** El acceso `Abrir CyberSOS` fuerza una base SQLite de desarrollo solo para sus procesos locales, crea las tablas antes de iniciar y comprueba mediante `/api/health` que la instancia usa SQLite. La URL de Supabase de `.env` no se modifica y se reserva para el servidor de despliegue. El endpoint de salud informa el tipo de base sin revelar datos sensibles.
- **Verificación final 2026-09-08:** Pytest 7/7 correcto con bases temporales; ESLint correcto; Vitest 4/4 correcto; build de Vite correcto. Se inició la aplicación local y se registró un reporte ficticio de integración (`201`), mientras `/api/health` confirmó `status: ok` y `database: sqlite`. Los registros de verificación temporales permanecen dentro de `.runtime`, ignorado por Git.

- **Actualización 2026-09-08 (robustez administrativa):** La bandeja permite filtrar también por prioridad y actualizar datos de forma manual, además de la sincronización automática. El detalle distingue entre sesión vencida, caso inexistente y fallos temporales; al guardar cambios u observaciones ahora muestra el error en lugar de fallar silenciosamente. La capa HTTP conserva el código de estado para que la interfaz pueda tomar esas decisiones. Las pruebas usan SQLite temporal aislado y ya no dependen de la base configurada en `.env`.
- **Verificación 2026-09-08:** ESLint correcto; Pytest 7/7; `GET /api/health` respondió `{"status":"ok"}`; escaneo de secretos sin hallazgos y `.env` ignorado por Git. Vitest y el build de Vite continúan bloqueados por el permiso de lectura de esbuild sobre el directorio superior de OneDrive, no por una falla detectada en la interfaz.

- **Fecha:** 2026-09-07
- **Objetivo actual:** Transformar CyberSOS de una demostración de piloto a una plataforma institucional de ciberdenuncias.
- **Terminado:** Se retiraron la pantalla y API de código de piloto, Gemini y su consentimiento. El formulario web recibe denuncias sin cuenta ciudadana; la cuenta administrativa sigue siendo única y exige MFA. La bandeja actualiza reportes y métricas automáticamente cada 15 segundos. Se inició una nueva identidad visual violeta y magenta.
- **Pendiente:** Verificar frontend, backend y flujo en navegador; añadir controles antiabuso antes de admitir datos reales. No se debe habilitar registro público de administradores ni ciudadanos.
- **Decisión:** La tabla histórica de análisis IA no se elimina en esta entrega para evitar una migración destructiva; ya no hay endpoint ni interfaz que la use.
- **Pruebas:** Pytest 6/6 correcto y ESLint correcto. La prueba visual de esta carpeta quedó bloqueada porque `localhost:5173` está atendido por otra copia antigua ubicada en `Apolo`; su proceso no se modificó. Vite/esbuild también encuentra restricciones de lectura del entorno al compilar desde OneDrive.
- **Actualización 2026-09-07:** Se creó el símbolo SVG propio de CyberSOS, la guía de identidad “Vigilancia Violeta”, y métricas de categorías, prioridades y promedio de gestión para el panel. Se añadió un límite en memoria de 5 envíos por IP cada hora para el formulario público. Resultado: Pytest 7/7 y ESLint correcto. Este límite es una primera defensa para desarrollo y debe complementarse con controles perimetrales antes de producción.
- **Actualización 2026-09-07 (acceso):** Se incorporó una ilustración original de baliza protegida por una red digital en el inicio de sesión administrativo. El formulario conserva etiquetas, contraste y acceso directo a creación de cuenta. La vista se comprobó en navegador en `http://127.0.0.1:4174/admin/login`; ESLint correcto.
- **Actualización 2026-09-07 (diseño de acceso):** El acceso administrativo se reorganizó siguiendo una composición institucional: identidad e ilustración a la izquierda y tarjeta de inicio de sesión a la derecha. En pantallas pequeñas pasa a una sola columna, mantiene controles táctiles amplios y evita desplazamiento horizontal. La creación de cuenta sigue limitada al único administrador autorizado.
- **Actualización 2026-09-07 (ilustración de acceso):** El panel visual del acceso incorpora una retícula digital, nodos de red, luz ambiental e indicador de reportes protegidos sobre la ilustración. Los elementos decorativos se ocultan en móvil para mantener la pantalla clara y ligera.
- **Actualización 2026-09-08 (arranque y reportes):** Los accesos locales ahora inician la versión actual en `http://localhost:4174`, permiten CORS desde ese puerto y crean las tablas SQLite de desarrollo antes de arrancar FastAPI. Se verificó `GET /api/health`, un reporte ficticio válido (`201`) y el mensaje específico para un correo inválido (`422`). Los intentos inválidos ya no consumen el límite antiabuso.
- **Actualización 2026-09-08 (WhatsApp):** El panel QR conserva credenciales administrativas al consultar FastAPI, muestra el carácter local del canal y explica la indisponibilidad de la central sin revelar información sensible. Las tablas de WhatsApp quedan incluidas al inicializar SQLite para desarrollo. El QR real requiere configurar PostgreSQL/Supabase, `SUPABASE_SERVICE_ROLE_KEY`, `WHATSAPP_SESSION_ENCRYPTION_KEY` y `WHATSAPP_WEBHOOK_SECRET` únicamente en `.env`; no hay valores de esos secretos en esta copia, por lo que no se inició ni vinculó una cuenta.
- **Actualización 2026-09-08 (formulario público):** Cada categoría de incidente ahora incluye una explicación breve y no técnica —por ejemplo, phishing, suplantación, extorsión y malware— para que una persona pueda elegir su caso sin conocer vocabulario especializado.

- **Fecha:** 2026-09-05
- **Objetivo actual:** Completar la integración local de WhatsApp y Gemini con datos ficticios.
- **Terminado:** Se instaló la skill `integrar-whatsapp-baileys` y se añadió el puente Baileys, almacenamiento privado, outbox, eventos firmados, panel local protegido por sesión administrativa y migración revisable. Los mensajes directos nuevos crean o actualizan un reporte de origen WhatsApp y dejan historial. Gemini propone categoría, prioridad, resumen y señales con salida estructurada; no recibe datos de contacto, exige consentimiento y nunca modifica el caso por sí mismo.
- **Pendiente:** Aplicar la migración `20260831000000_create_whatsapp_central.sql` mediante Supabase MCP, configurar secretos solo en `.env`, instalar/probar el puente y enlazar una cuenta dedicada. Configurar una clave Gemini local para la prueba real. No conectar números ni usar datos reales hasta contar con aprobación.
- **Actualización 2026-09-05:** La migración `20260831000000_create_whatsapp_central.sql` se aplicó manualmente en el SQL Editor del proyecto Supabase de desarrollo `byqympyzppstmgofqohs`. La consola devolvió éxito y la consulta posterior mostró tablas `whatsapp_*`, incluido `whatsapp_connections`; no se conectó ningún número. Verificación local posterior: comprobador de la integración correcto, Pytest 5/5 y puente TypeScript 2/2. La verificación local del frontend quedó pendiente: el runtime aislado no pudo resolver `frontend/vite.config.js` por permisos de lectura del directorio superior, pese a que el archivo existe.
- **Actualización de verificación 2026-09-05:** Se añadió una prueba automática del webhook: con un evento ficticio firmado, FastAPI devuelve 202, evita procesar por segunda vez el mismo `event_id` y crea un reporte de origen WhatsApp. Resultado actual: Pytest 6/6, puente TypeScript 2/2, ESLint correcto, Vitest 3/3, build de Vite correcto y `/api/health` respondió `{"status":"ok"}` en localhost. Se corrigió `frontend/pnpm-workspace.yaml` para permitir explícitamente el binario bloqueado de `esbuild` durante el build local. `.env` sigue fuera de Git y el escaneo no detectó secretos fuera de `.env.example`.
- **Pruebas:** `pytest` 5/5; comprobador de la skill de WhatsApp correcto; SDK Google GenAI importado correctamente. Frontend: ESLint correcto, Vitest 3/3 y build de Vite correcto. Puente: TypeScript correcto y pruebas Node 2/2. La integración con una cuenta y Gemini real no se ejecutó porque todavía no hay migración aplicada ni secretos configurados.
- **Bloqueos:** El MCP de Supabase no está disponible en esta sesión para aplicar la migración. Node quedó disponible mediante pnpm y el puente está verificado; no se usa `npm` porque no está instalado en el runtime.

- **Fecha:** 2026-08-28
- **Objetivo actual:** Enrolar el factor TOTP y completar la prueba real del acceso administrativo con MFA.
- **Terminado:** Dependencias, UI/UX Pro Max 2.15.0, esquema privado aplicado en Supabase, API de piloto/reportes, número de caso, portada dinámica, bandeja administrativa, detalle e historial. Supabase Auth quedó integrado con cookies seguras, enrolamiento TOTP y exigencia de nivel `aal2`. El 2026-08-27 se autorizó e invitó a `santiagoforeroch@gmail.com` como único administrador del panel y se confirmó que también es propietario técnico de APOLO.
- **Terminado adicional:** Flujo de evidencias con validación de firma, límite de 10 MB, máximo cinco archivos, SHA-256, nombres aleatorios, bucket privado y enlaces de descarga de 60 segundos. El detalle administrativo ya presenta las evidencias. Se aplicaron índices para evidencias, análisis, observaciones e historial.
- **Terminado adicional el 2026-08-28:** La clave secreta del backend quedó configurada solo en `.env`. Se verificó contra Supabase una carga ficticia, un enlace temporal, la descarga con contenido idéntico y la limpieza del archivo de prueba.
- **Terminado adicional de autenticación:** Una consulta administrativa de solo lectura confirmó que el administrador existe y que su correo ya está confirmado. No se mostraron identificadores ni datos sensibles.
- **Pendiente:** Enrolar el factor TOTP y completar la prueba real con MFA, activar antivirus antes de datos reales, análisis Gemini, webhook de WhatsApp y retención automática.
- **Pruebas:** Pytest 2/2 con SQLite temporal (incluye `/api/health`, evidencia válida, rechazo de archivo falso y enlace temporal), Vitest 2/2, ESLint sin advertencias y build de producción correcto. La prueba real del bucket privado completó carga, URL firmada, descarga íntegra y limpieza. El escaneo no detectó secretos y `.env` sigue ignorado por Git.
- **Seguridad:** El asesor de Supabase detecta protección contra contraseñas filtradas desactivada; debe evaluarse antes de producción. `.env` permanece ignorado y el escaneo no mostró secretos expuestos.
- **Acceso actualizado:** `santiagoforeroch@gmail.com` sustituyó al correo institucional como único administrador autorizado del panel y recibió una invitación de Supabase Auth. El usuario institucional anterior permanece sin autorización en la aplicación.
- **Bloqueos:** La verificación de solo lectura encontró cero factores MFA registrados. El administrador debe completar personalmente el enrolamiento TOTP; ninguna contraseña o código debe compartirse por chat.
- **Siguiente paso:** Abrir el panel, completar el enrolamiento TOTP y probar el acceso real; después, incorporar análisis antivirus.

No guardar secretos en este archivo.

- **Actualización 2026-09-14 (base del agente conversacional):** Se aprobó ampliar CyberSOS con un agente ciudadano natural. Se añadió configuración privada para Gemini (`GEMINI_API_KEY`, `GEMINI_MODEL`, `AI_AGENT_ENABLED`), contrato Pydantic para mensajes y borrador estructurado, y el endpoint `POST /api/v1/agent/chat`. La IA se invoca exclusivamente desde FastAPI, no accede directamente a Supabase y no crea reportes sin una futura confirmación explícita. Sin clave o con el agente desactivado responde con un respaldo seguro hacia el formulario actual. Pendiente: interfaz React del chat, persistencia de conversaciones, endpoint de confirmación que reutilice `create_report`, pruebas, límites de uso y configuración privada en desarrollo/Vercel. Verificación: `pytest` no pudo ejecutarse porque el entorno actual no tiene el módulo instalado; falta ejecutar la comprobación completa en el entorno del proyecto.

- **Actualización 2026-09-12 (MCP con token privado):** Se configuró el servidor MCP del proyecto con alcance exclusivo a `kmjlwaviqqznjagrgkpd` y la referencia a la variable de usuario `SUPABASE_ACCESS_TOKEN`. El token no está en el repositorio, archivos de entorno ni este registro. Falta que Codex recargue la configuración y comprobar la disponibilidad de las herramientas antes de aplicar las migraciones existentes.

- **Actualización 2026-09-12 (Vercel):** Se creó `CRON_SECRET` como variable privada de **Production** en Vercel y se generó un nuevo despliegue de producción. El despliegue `43sto5SgBKtTPKs23sKqrd6o9VbY` terminó en estado **Ready** y está asignado a `cybersos-web.vercel.app`. No se ejecutó manualmente la tarea de retención: esa operación elimina únicamente datos ficticios vencidos y debe ser invocada por el programador autenticado. Queda pendiente publicar el código local verificado (incluye la protección de dicha tarea) y aplicar las migraciones al nuevo Supabase mediante MCP.
- **Actualización 2026-09-15 (modo temporal sin API keys):** El endpoint `POST /api/v1/agent/chat` ahora usa un flujo determinista local cuando Gemini/OpenAI están desactivados o no tienen clave. Reconoce categorías y prioridades con palabras clave, recopila hasta cinco hechos, solicita fecha/canal/evidencias faltantes y solo marca el borrador listo después de al menos dos mensajes y una categoría detectada. La creación continúa requiriendo nombre, contacto y confirmación ciudadana. Prueba aislada: `tests/test_agent.py` 3/3; la batería completa quedó limitada por permisos de Windows en la carpeta temporal de Pytest.
- **Actualización 2026-09-15 (activación local):** Se añadió `scripts/configurar_admin_local.ps1`. Solicita la contraseña y el código MFA sin mostrarlos, y actualiza únicamente el archivo `.env` ignorado por Git. El correo local predeterminado es `admin@cybersos.example`; no se guardó ninguna contraseña en el repositorio ni en este handoff.
- **Actualización 2026-09-15 (recuperación de contraseña):** Se añadió el botón de recuperación en el login, el formulario para solicitar el correo y la pantalla para definir una nueva contraseña. El backend usa Supabase Auth y construye el destino con el dominio público de la solicitud para evitar enlaces `localhost` en Vercel. Verificación: ESLint correcto, compilación Python correcta y `tests/test_agent.py` 3/3.

- **Actualización 2026-09-15 (chat temporal en producción):** El chat ciudadano ya no intenta guardar conversaciones en Supabase durante el modo temporal sin API keys; recibe el historial desde el navegador y genera el borrador estructurado localmente. Se promovió el despliegue de producción `dpl_797dRVAGszL8ks1puVsiJorpdwDQ`, asignado a `https://cybersos-web.vercel.app`. Verificación externa: `POST /api/v1/agent/chat` respondió HTTP 200 con mensaje seguro y borrador de reporte. No se usaron ni registraron secretos.
